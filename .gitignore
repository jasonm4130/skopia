# Dependencies
node_modules/
.pnp/
.pnp.js

# Build output
dist/
build/
.wrangler/
.dev.vars
.dev.vars.*
!.dev.vars.example

# Env / secrets
.env
.env.*
!.env.example

# Cloudflare
wrangler.toml.bak

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
*.swp

# Test / coverage
coverage/
.vitest/

# Session recaps — local working notes, never committed
docs/recaps/

# Playwright MCP — local browser snapshots/artifacts, never committed
.playwright-mcp/

# Claude Code — local-only settings (machine-specific paths, never committed)
.claude/settings.local.json

# Skopia foundation (node_modules, dist, .wrangler, coverage, *.log already
# covered above; this entry is the wrangler-generated types file).
worker-configuration.d.ts

# Session handoff docs (local-only)
/.claude/handoffs/
